# TODO: It does not work.
#set(CMAKE_INSTALL_PREFIX /)
FILE(GLOB CONFIGS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} "*.conf")
install(FILES ${CONFIGS} DESTINATION ${CMAKE_INSTALL_SYSCONFDIR}/${CMAKE_PROJECT_NAME})
